# Update 2026-07-22

## Fixed
- Plan upload middleware: added `upload.single('planFile')` to `POST /api/plans/upload` in planRoutes.js
- Branch code in user public-list: `getPublicUsersList` now includes `branch_code` in response
- Auto-NPL daily snapshot via `node-cron` in server.js (runs daily at 02:00)

## Added
- Staff NPL dashboard page (`/npl/staff`) — PAR summary cards, collection rate, overdue alerts
- Supervisor mapped accounts team aggregate — "Team Total Accounts" stat card
- June Balance Difference page (`/june-difference`) for staff & supervisors — account-level balance comparison with growth breakdown
- Unmapped accounts endpoint (`GET /api/mapped-accounts/unmapped`) + frontend page (`/unmapped-accounts`) for admin/area manager/branch manager — accounts without staff assignment, with branch filter, pagination, summary stats

## Pending (next)
- Reports page with period picker (monthly/quarterly/half-yearly/yearly) + print for admin/area/BM
- CBS matching improvements with real data

## Notes
- All changes deployed to production
- Login route moved from `/api/users/login` to `/api/auth/login` (frontend already updated)
- Mapped accounts route changed: dashboard at `/api/mapped-accounts/dashboard` (not `/api/mapped-accounts`)
